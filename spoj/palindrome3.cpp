#include<cstdio>
#include<string>
using namespace std;
int main()
{
	int n, i;
	char ch[30001];
	int f = true;	
	scanf("%d",n);	
	for ( i = 0 ; i < n/2 ; i ++ ) {
		ch[i] = getchar();
	}
	for ( i = n/2 ; i < n ; i ++ ) {
		ch[i] = getchar()
		if( ch[i] != ch[n-i])
			flag = false;
	}
	if(flag)
}
